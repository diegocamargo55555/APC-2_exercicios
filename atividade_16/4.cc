#include <iostream>

const double PI = 3.14159;

int main() {
    double radius = 5.0; // Raio do círculo

    // Calcula a área do círculo usando a constante PI
    double area = PI * radius * radius;

    std::cout << "A área do círculo é: " << area << std::endl;

    return 0;
}
